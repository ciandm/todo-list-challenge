import React from 'react';
import * as S from './styled';

const PageLoader = () => {
  return (
    <S.Container>
      <S.Loader>Loading...</S.Loader>
    </S.Container>
  );
};

export default PageLoader;
