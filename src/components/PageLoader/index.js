import React from 'react';
import Loader from './Loader';
import * as S from './styled';

const PageLoader = () => {
  return (
    <S.Container>
      <Loader />
    </S.Container>
  );
};

export default PageLoader;
